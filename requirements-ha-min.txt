# pytest-homeassistant-custom-component 0.13.171 pins Home Assistant 2024.10.0,
# the minimum release declared in hacs.json.
pytest-homeassistant-custom-component==0.13.171

# The 2024.10 dependency graph predates josepy 2.x and pycares 4.5+;
# without historical constraints, a fresh resolver produces an incompatible ACME
# import and a background DNS shutdown thread rejected by the legacy test plugin.
josepy==2.2.0
pycares==5.0.1